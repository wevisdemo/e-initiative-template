---
interface Props {
	id?: string;
	outerClass?: string;
	innerClass?: string;
}

const { id, outerClass, innerClass } = Astro.props;
---

<div
	{id}
	class={`w-full flex flex-col items-center px-3 py-12 md:py-24 ${outerClass || ''}`}
>
	<div class={innerClass}>
		<slot />
	</div>
</div>
